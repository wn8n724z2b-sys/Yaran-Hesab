{
  "name": "hesabdari-asan-free",
  "private": true,
  "version": "3.2.0",
  "description": "حسابداری آسان 3.2 — نسخه رایگان پایدار Windows Native",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm run prepare-font && tauri dev",
    "build": "npm run prepare-font && tauri build",
    "prepare-font": "node scripts/prepare-vazirmatn.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@fontsource-variable/vazirmatn": "5.3.0"
  }
}